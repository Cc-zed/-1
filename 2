#include <iostream>
#include <cmath>
using namespace std;

/*-----------------------Task 1-----------------------
 
double calculateEquivalentResistance(double R1, double R2, double R3) {
    return 1 / (1 / R1 + 1 / R2 + 1 / R3);
}

int main() {
    double R1, R2, R3;


    cout << "Enter R1: ";
    cin >> R1;
    cout << "Enter R2: ";
    cin >> R2;
    cout << "Enter R3: ";
    cin >> R3;

 
    double R0 = calculateEquivalentResistance(R1, R2, R3);


    cout << "Result R0 = " << R0 << endl;

    return 0;
}*/


/*-----------------------Task 2-----------------------

int main() {
    double L;
    const double pi = 3.14;

    cout << "Enter length of circle: ";
    cin >> L;

    double R = L / (2 * pi);

    double S = pi * pow(R, 2);

    cout << "Radius: " << R << endl;
    cout << "Plosha: " << S << endl;

    return 0;
}*/



/*-----------------------Task 3-----------------------

double calculateDistance(double v, double t, double a) {
    return v * t + (a * t * t) / 2;
}

int main() {
    double v, t, a;

    cout << "Enter shvudkist (v): ";
    cin >> v;
    cout << "Enter time (t): ";
    cin >> t;
    cout << "Enter pruskhorennya (a): ";
    cin >> a;

    double S = calculateDistance(v, t, a);

    cout << "Proydena vidstan: " << S << endl;

    return 0;
}*/

//----------------------- END -----------------------
